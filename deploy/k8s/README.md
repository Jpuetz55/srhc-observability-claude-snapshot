# Kubernetes manifests

This is a minimal Kustomize layout for the Grafana/Mimir monitoring stack.

- `base/` shared resources
- `overlays/dev/` editable development profile
- `overlays/prod/` production profile
