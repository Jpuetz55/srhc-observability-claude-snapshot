# Development overlay reuses the base manifests and tags every object for
# environment-specific inventory and selection.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
commonLabels:
  environment: dev
